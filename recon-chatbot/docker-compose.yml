services:
  recon-chatbot-api:
    build: .
    container_name: recon-chatbot-api
    env_file:
      - .env
    environment:
      - RECON_URL=http://host.docker.internal:9888
    ports:
      - "5001:5001"
    volumes:
      - .:/app
    command: ["python", "api_server.py"]
